# 完善比赛结果显示 - 显示所有子组

## 📋 需求说明

在之前的修复中，比赛结果页面只显示**有结果的子组**。如果某个子组还没有产生结果，该子组就不会出现在列表中。

**用户期望**：显示所有子组，包括还没有产生结果的子组，并提示"该组暂无结果"。

## 🔍 问题分析

### 原有逻辑
`loadResults` 方法只调用 `getResults` 云函数，返回的数据结构：
```javascript
[
  {
    sub_group_id: "high_school_male_w55",
    sub_group_name: "高中组男子组-55kg级",
    results: [{ student_name: "王伟", ... }]
  }
  // 只包含有结果的子组
]
```

### 问题
如果某个子组没有任何结果记录，它就不会出现在返回数据中，导致前端无法显示。

## ✅ 解决方案

### 核心思路
1. **获取所有子组列表**（`competition_sub_groups` 集合）
2. **获取结果数据**（`competition_results` 集合）
3. **合并数据**：将结果数据匹配到子组列表中
4. **渲染**：即使某个子组的 `results` 数组为空，也会显示"该组暂无结果"

## 🔧 代码修改

### 1. 修改云函数 `getSubGroups` 方法

添加 `flat` 参数，支持返回扁平的子组列表：

```javascript
async function getSubGroups(event) {
  const { competition_id, flat = false } = event;
  
  if (!competition_id) {
    return {
      success: false,
      message: '缺少比赛ID'
    };
  }
  
  try {
    const result = await db.collection('competition_sub_groups')
      .where({ competition_id })
      .orderBy('age_group_code', 'asc')
      .get();
    
    // 如果需要扁平列表，直接返回
    if (flat) {
      return {
        success: true,
        data: result.data
      };
    }
    
    // 按年龄组分组（原有逻辑）
    const grouped = {};
    result.data.forEach(subGroup => {
      const key = subGroup.age_group_name;
      if (!grouped[key]) {
        grouped[key] = {
          age_group_name: key,
          age_group_code: subGroup.age_group_code,
          sub_groups: []
        };
      }
      grouped[key].sub_groups.push(subGroup);
    });
    
    return {
      success: true,
      data: Object.values(grouped)
    };
  } catch (error) {
    console.error('获取子组列表失败:', error);
    throw error;
  }
}
```

**修改说明**：
- 新增 `flat` 参数（默认 `false`）
- 当 `flat = true` 时，直接返回所有子组的扁平数组
- 当 `flat = false` 时，保持原有的分组逻辑

### 2. 修改小程序 `loadResults` 方法

并行获取子组列表和结果数据，然后合并：

```javascript
loadResults: function() {
  this.setData({ loading: true });
  
  // 并行获取子组列表和结果数据
  Promise.all([
    // 获取所有子组（扁平列表）
    wx.cloud.callFunction({
      name: 'competitionFunctions',
      data: {
        type: 'getSubGroups',
        competition_id: this.data.competitionId,
        flat: true  // 获取扁平列表
      }
    }),
    // 获取结果数据
    wx.cloud.callFunction({
      name: 'competitionFunctions',
      data: {
        type: 'getResults',
        competition_id: this.data.competitionId
      }
    })
  ]).then(([subGroupsRes, resultsRes]) => {
    if (subGroupsRes.result?.success && resultsRes.result?.success) {
      const subGroups = subGroupsRes.result.data || [];
      const resultsData = resultsRes.result.data || [];
      
      // 创建结果映射（按 sub_group_id）
      const resultsMap = {};
      resultsData.forEach(group => {
        resultsMap[group.sub_group_id] = group.results || [];
      });
      
      // 合并子组和结果数据
      const mergedResults = subGroups.map(subGroup => ({
        sub_group_id: subGroup.sub_group_id,
        sub_group_name: subGroup.sub_group_name,
        results: resultsMap[subGroup.sub_group_id] || []
      }));
      
      this.setData({
        results: mergedResults
      });
    } else {
      wx.showToast({
        title: '加载失败',
        icon: 'none'
      });
    }
    this.setData({ loading: false });
  }).catch(err => {
    console.error('加载比赛结果失败:', err);
    wx.showToast({
      title: '加载失败',
      icon: 'none'
    });
    this.setData({ loading: false });
  });
},
```

**修改说明**：
1. 使用 `Promise.all` 并行调用两个云函数
2. 获取所有子组（`flat: true`）
3. 获取结果数据
4. 创建 `resultsMap` 映射表（key 是 `sub_group_id`）
5. 遍历所有子组，匹配结果数据：
   - 如果有结果，`results` 为结果数组
   - 如果没有结果，`results` 为空数组 `[]`
6. 前端模板会根据 `results.length` 判断显示结果还是"该组暂无结果"

## 📱 最终效果

```
📦 高中组男子组-55kg级
  🥇 王伟
     15岁 · 男 · 55kg
  🥈 李明  
     16岁 · 男 · 54kg

📦 高中组女子组-48kg级
  🥇 张三
     15岁 · 女 · 47kg
  
📦 初中组男子组-51kg级
  该组暂无结果        ← 子组存在但没有结果
  
📦 小学A组混合组-45kg级
  该组暂无结果        ← 子组存在但没有结果
```

## 🎯 优势

1. **完整性**：显示所有子组，不会遗漏
2. **清晰性**：用户能看到比赛的完整分组情况
3. **用户友好**：明确提示"该组暂无结果"，而不是悄无声息地不显示
4. **性能优化**：使用 `Promise.all` 并行请求，提高加载速度

## 🚀 部署步骤

1. **重新部署云函数** `competitionFunctions`
2. **在微信开发者工具中编译小程序**
3. **测试验证**：
   - 打开有多个子组的比赛详情
   - 切换到"比赛结果"tab
   - 验证所有子组都显示了
   - 验证没有结果的子组显示"该组暂无结果"

## 💡 扩展功能（可选）

如果希望只显示有结果的子组（类似原来的行为），可以在前端添加一个筛选开关：

```javascript
// 在 data 中添加
showEmptyGroups: true  // 是否显示空组

// 在合并数据时过滤
const mergedResults = subGroups
  .map(subGroup => ({
    sub_group_id: subGroup.sub_group_id,
    sub_group_name: subGroup.sub_group_name,
    results: resultsMap[subGroup.sub_group_id] || []
  }))
  .filter(group => this.data.showEmptyGroups || group.results.length > 0);
```

---

**修改完成！** 🎉 现在比赛结果页面会显示所有子组，包括还没有产生结果的子组。

